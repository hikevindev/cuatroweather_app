import { AuthForm } from '../components/AuthForm/AuthForm';

const Login = () => {
  return (
    <main>
      <section>
        <AuthForm />
      </section>
    </main>
  );
};

export default Login;
